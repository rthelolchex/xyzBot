const gi = require('@rthelolchex/genshininfo_scraper')
module.exports = async(m, { conn, text, command }) => {
    switch (command) {
        case 'gidailyclaim':
            break
        case 'gigetcookie':
            break
        case 'giredeemcode':
            break
        case 'gistalk':
            break
    }
}